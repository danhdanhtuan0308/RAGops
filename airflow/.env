AIRFLOW_IMAGE_NAME=apache/airflow:2.9.2
AIRFLOW_UID=50000
AIRFLOW_GID=50000
AIRFLOW_HOME=/opt/airflow
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=airflow
# PIP_ADDITIONAL_REQUIREMENTS is no longer used (we install with uv from pyproject.toml)
# PIP_ADDITIONAL_REQUIREMENTS=
# Set this to the absolute path of your GCP service account key on the HOST.
# Compose mounts it into containers at /opt/airflow/keys/google-credentials.json
# Example (edit to your path):
GOOGLE_APPLICATION_CREDENTIALS=/Users/you/keys/your-sa.json
